[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.pdo_mysql"]

[phases.build]
cmds = ["composer install --no-dev --optimize-autoloader"]

[start]
cmd = "sleep 5 && php artisan migrate:fresh --seed --force && php artisan serve --host=0.0.0.0 --port=$PORT"
